# This file is managed by puppet.
#
PRUNEFS = "<%= ((@prunefs << @extra_prunefs).flatten!.sort).join(' ') %>"
PRUNEPATHS = "<%= ((@prunepaths << @extra_prunepaths).flatten!.sort).join(' ') %>"
<% if @prunenames -%>
PRUNENAMES = "<%= ((@prunenames << @extra_prunenames).flatten!.sort).join(' ') %>"
<% end -%>
<% if @prune_bind_mounts -%>
PRUNE_BIND_MOUNTS = "<%= @prune_bind_mounts.to_s %>"
<% end -%>
