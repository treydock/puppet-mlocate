---
version: 5
defaults:
  datadir: data
  data_hash: yaml_data
hierarchy:
  - name: "Common defaults"
    path: "common.yaml"
  - name: "RedHat Major Versions"
    path: "%{facts.osfamily}_%{facts.operatingsystemmajrelease}.yaml"
